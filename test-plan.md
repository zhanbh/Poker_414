# 414 工程审查测试计划

## 用户确认更新（优先于下方原始待定案例）

- E1：报爆后允许拆牌首出/跟牌；例如剩 `AA` 遇单张 `K` 可拆出一张 `A`。拆牌后若剩余两张及以上再次满足爆牌条件，需重新确认；只剩一张可单出的牌自动爆；不能管则过。
- E2：普通局第三名出完后尾家自动记第四名，即使余王无法出；立棍只统计未弃牌玩家，尾家自动末位。
- E3：A以最后55差掉5，仍持牌的队友C接风；A普通牌出完无人管同样C接风。A单5出完后B出6合法，则继续当前轮，不提前给C首牌。A、C拿前两名时直接抓两家结算并升2，不再要求B/D出牌，不虚构两者相对名次。检查终局先于接风，不把牌权交给出完/弃牌者。下一局首牌仍归本局第一名。
- E4：正式验收为J+1=Q、J+2=K，达到/超过A完成一轮并重置3。
- E5：首牌候选开始逐人立棍/跳过，全部跳过才正常开打；有人立棍后先给队友抢立，确定立棍方后仅对方反立。普通等级下立棍成功本方+4/对面不变、失败本方-4/对面+4；反立成功本方+8/对面不变、失败本方-8/对面+8，不叠加4级。等级5立棍失败只到3；等级3再败仍为3；任一失败队等级J优先回3，即使挑战者成功不对其施加普通扣级。

日期：2026-09-10。来源：tasks.md及requirements-eng-review.md。尚无实现，以下均为待执行验收。E1–E5以上方最新决议为准；E6–E10已全部接受，以下协议、E2E和生命周期用例是实现要求，不表示已测试通过。

## 纯规则

- 主3：5→6合法，5→7普通跟牌非法，5→2合法，2→3合法，3→2非法。
- 主5：主5不能被普通6管；345与456内的5仅按连续点数判定。
- 主3：5王（配5）→55允许同点数真牌覆盖；55→77非法；55→66合法；相同真牌不能互管。
- 主8：345为顺子，34不足三张；334455为连对，3344不足三对；连续牌含2或跨A回3拒绝。
- 主8：34王（配5）与天然345按同牌面真牌优先判定；345→456合法，345→567非法；三张顺子不能被四张普通顺子管。
- 主3：555王为普通4炸，666不能管；主5时555王为主4炸。逐项覆盖全部炸牌阶梯及连对最低压制门槛。
- 44A所有排列结果相同；4王A/王王A可配414；44王不可配414。王在普通AA王中可以配A，王单出非法。
- 同点数对子回应普通单牌、2、主均可差；回应对子不可差；差不可主动首出。
- 拒绝空选牌、重复实体牌ID、非本人牌、伪造王替代目标；非法动作不扣牌。

## 状态机与计分

- 一副牌发完54张、无重复无遗漏、两人13张两人14张；固定随机输入得到可重复结果。
- ABCD：AC抓一家+1，下局A首牌；ABDC：平局，下局A；BACD：平局，下局B；ACBD：AC抓两家+2。
- AC等级5、BD等级7，本局A首牌锁主5；BD获胜+1应从7到8。
- J+1=Q、J+2=K已确认；J输回3、平不变；到达/超过A只增加该队轮数并重置3。
- E1：`AA` 报爆后拆出一张 `A`、以及拆牌后再次确认或最后一张自动爆的合法性与UI提示；必须证明不存在无合法首出动作的承诺状态。
- E2：普通模式尾家只剩王；立棍/反立弃牌者不再出牌时仍可结算。
- E3：A用最后55差掉5后谁首出；A最后一手无人管后谁首出；已出完/弃牌玩家跳过；过牌集合在新出牌后重置。
- E5：全员不立、立棍成功、立棍失败、反立成功、反立失败、同时声明和协商中刷新。每分支必须有最终主、有效玩家集合和奖励结果。

## 服务协议与隐私

- 正确/错误邀请码、重复昵称、第五人、并行创建唯一房间；所有拒绝不改变状态。
- 同请求ID重复提交只应用一次；ACK丢失后重试返回一致结果；上局动作延迟送达不能作用于新局。
- 重连恢复本人视图，其他玩家响应、广播及health均不包含他人手牌或私密令牌。
- 同会话第二标签页接管原座位，第一标签页停止操作并显示接管提示；伪造公开玩家ID不能取得身份；重启后旧会话返回重新登录提示。
- 房主断线保留权限、不自动转让；彻底丢失会话后按文档重启服务才能恢复大厅，原牌局丢失。
- 点击选牌恢复活动标识，单纯Socket心跳不能解除暂离；无操作1分钟仅提示，不自动过牌。
- 非房主移除/开始/重开/结束拒绝；开局前房主移除有效；进行中重开拒绝；结算后重开重置等级/轮数。

## E2E（真实构建服务，独立于单元测试阶段）

- 四个独立浏览器身份通过HTTP页面登录→入座→房主开始→首牌动画→立棍协商→首出/过牌→结算→下一局。
- 发牌后刷新、自己回合断线恢复、报爆后刷新，均恢复正确座位、手牌和状态。
- 检查实际网络消息中没有他人私密手牌；直接访问未认证页面/连接不能进入对局。
- 桌面布局可选牌和取消选择，多解释选择不会误出；服务器拒绝后手牌不丢失。
- 构建静态资源与实时连接同源可达；服务重启丢失内存牌局时显示可理解的重新进入路径。

## 生命周期

- 多次连接/断开不累计监听器；房间结束清理快照、会话、去重记录与定时器。
- 报爆只检查完整余牌；活动提示不触发规则重算。无数据库性能或大规模负载目标。
